server:
  port: 8084

spring:
  application:
    name: weather_report_eureka

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/