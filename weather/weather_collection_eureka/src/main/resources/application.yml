server:
  port: 8081

spring:
  application:
    name: weather_collection_eureka

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
