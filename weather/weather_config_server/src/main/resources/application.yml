server:
  port: 8888
spring:
  application:
    name: weather-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/VioletKiss/springConfigTest
          search-paths: repo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/